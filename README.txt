install python and anaconda on your local machine

Navigate to the folder using your code editor

Run the command in the terminal to install the required packages

pip install -r requirements.txt

execute the app.py file using the command

"Path to your anaconda folder(must be in users/username)"/anaconda3/envs/myenv/bin/python3 app.py

